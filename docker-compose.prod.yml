services:
  backend:
    build:
      context: ./Madnolia-Backend
      dockerfile: Dockerfile
      target: production
    ports:
      - "3000:3000"
    environment:

      - NODE_ENV=production
    restart: always
    # No volumes for source code in production to keep it immutable
    # Only volumes for persistent data (like uploads) would go here
